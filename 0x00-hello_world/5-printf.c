#include <stdio.h>

/**
 * main prints the string 
 *return 0 if and only if program runs properly
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
